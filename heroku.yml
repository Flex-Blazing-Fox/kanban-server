setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - bash deployment-task.sh 
run:
  web: node index.js       